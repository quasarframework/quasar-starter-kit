const DevServer = function (webpackConfig) {
  this.config = webpackConfig
}

DevServer.prototype.build = function () {
  //
}

DevServer.prototype.stop = function () {
  //
}

module.exports = DevServer
