#!/usr/bin/env node

// cli deps
const
  parseArgs = require('minimist'),
  path = require('path'),
  chalk = require('chalk')

const
  log = require('../lib/helpers/logger')('app:info')
  appPaths = require('../lib/build/app-paths')

const argv = parseArgs(process.argv.slice(2), {
  // TODO
})

if (argv.help) {
  console.log(`
    Description
      Add support for building in certain modes.
    Usage
      $ npm run add
  `)
  process.exit(0)
}
